import { EquipeTableSkeleton } from './equipe-table-skeleton';

const Loading = () => {
    return (
        <div className="space-y-6">
            <EquipeTableSkeleton />
        </div>
    );
};

export default Loading;
